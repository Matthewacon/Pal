package io.github.matthewacon.pal.api;

import java.lang.annotation.Annotation;

//public interface IPalProcessor<T extends Annotation & IPalAnnotation<T>> {
public interface IPalProcessor<T extends Annotation> {}
